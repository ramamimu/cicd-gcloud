steps:
  # build image and push to gcr.io
  - name: "gcr.io/cloud-builders/docker"
    args: ["build", "-t", "gcr.io/$PROJECT_ID/nginx-cicd:latest", "."]
  - name: "gcr.io/cloud-builders/docker"
    args: ["push", "gcr.io/$PROJECT_ID/nginx-cicd:latest"]

  # apply cluster configuration
  - name: "gcr.io/cloud-builders/kubectl"
    args:
      - "apply"
      - "-f"
      - "deployment.yaml"
    env:
      - "CLOUDSDK_COMPUTE_ZONE=asia-southeast2"
      - "CLOUDSDK_CONTAINER_CLUSTER=nginx-cluster"
